terraform {
  required_version = ">= 1.5.0"

  required_providers {
    aws = {
      source  = "hashicorp/aws"
      version = "~> 6.0"
    }
    local = {
      source  = "hashicorp/local"
      version = "~> 2.5"
    }
  }

  # Uncomment after creating the bucket. Backend blocks cannot use variables.
  # backend "s3" {
  #   bucket  = "pathnex-devops-terraform"
  #   key     = "batch/may/kafka/terraform.tfstate"
  #   region  = "ap-south-1"
  #   encrypt = true
  #   use_lockfile = true
  # }
}

